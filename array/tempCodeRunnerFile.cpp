nt> current_level;
        // int size = q.size();
        // for (int i = 0; i < size; i++)
        // {
        //     node *current = q.front();
        //     q.pop();
        //     current_level.push_back(current->val);
        //     if (current->left)
        //     {
        //         q.push(current->left);
        //     }
        //     if (current->right)
        //     {
        //         q.push(current->right);
        //     }
        // }
        // level_wi